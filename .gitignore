node_modules
dist
.DS_Store
*.local
.vite
node_modules/.tmp

# TypeScript / Vite build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Firebase deploy cache (regenerated on every deploy)
.firebase/

# Teller — NEVER commit bank certificates, keys, or worker build state
.forbord/
*.pem
*.key
worker/.wrangler/
worker/node_modules/
